# FinGuard 不需要在 .env 中配置真实密钥。
# 模型的 API Key / Base URL / Model Name 均通过页面右上角“模型设置”填写，
# 仅保存在浏览器当前页面运行状态中，随分析请求发送到本地的 Next.js API Route，
# 由服务端使用该配置调用一次模型接口，不写入日志、不落盘、不提交到代码仓库。
#
# 以下变量当前程序不会读取，仅作为安全示例保留，说明本项目不依赖预置密钥即可运行。
MODEL_API_KEY=
MODEL_BASE_URL=
MODEL_NAME=
